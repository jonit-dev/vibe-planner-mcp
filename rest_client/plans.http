### Create a new plan with all fields
POST http://localhost:3000/api/plans
Content-Type: application/json

{
  "name": "My Test Plan from REST Client",
  "description": "This is a detailed description for the plan created via .http file.",
  "status": "pending"
}

### Create a new plan with only the required name
POST http://localhost:3000/api/plans
Content-Type: application/json

{
  "name": "Minimal Test Plan"
}

### Create a plan with an invalid status (to test error handling or default)
POST http://localhost:3000/api/plans
Content-Type: application/json

{
  "name": "Plan with Invalid Status",
  "description": "Testing what happens with a bad status value.",
  "status": "super_custom_bad_status"
}

### Create a plan with a missing name (to test error handling)
POST http://localhost:3000/api/plans
Content-Type: application/json

{
  "description": "This plan is missing a name."
}

### Get all plans
GET http://localhost:3000/api/plans

### Get a specific plan by ID
# Note: Replace {{planId}} with an actual ID from a created plan
GET http://localhost:3000/api/plans/{{planId}}

### Get all phases for a specific plan
# Note: Replace {{planId}} with an actual ID from a created plan
GET http://localhost:3000/api/plans/4fde4f87-a556-4b33-a0a3-35a6f8afceb8/phases

### Create a new phase in a plan
# Note: Replace {{planId}} with an actual ID from a created plan
POST http://localhost:3000/api/plans/4fde4f87-a556-4b33-a0a3-35a6f8afceb8/phases
Content-Type: application/json

{
  "name": "Phase 1: Design",
  "description": "Initial design and mockups for the project.",
  "order": 1
}

### Create a new task
# Note: Replace {{phaseId}} with an actual ID from a created phase
POST http://localhost:3000/api/tasks
Content-Type: application/json

{
  "phaseId": "20980730-8df9-4a43-bc7b-b5ba603d1f2f", 
  "name": "Create wireframes",
  "description": "Develop detailed wireframes for all screens.",
  "order": 1
}

### Create another task in the same phase
# Note: Replace {{phaseId}} with an actual ID from a created phase
POST http://localhost:3000/api/tasks
Content-Type: application/json

{
  "phaseId": "{{phaseId}}",
  "name": "User testing for wireframes",
  "description": "Conduct user testing sessions with the created wireframes.",
  "order": 2
}
